import * as React from 'react';
import { Button } from '@chakra-ui/button';
import ReactSelect from 'react-select';

export const MyButton = () => <Button colorScheme="green" />;

export const MyCustomSelect = () => <ReactSelect />;
